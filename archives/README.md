# Archives (not part of the active book)

This tree holds **retired** code. Do **not** import these modules from `marimo_notebooks/`.

| Path | What it was |
|------|-------------|
| `old-pipeline/` | Early Jupyter step folders (`I_preprocessing`, `II_deeplearning`, `III_brain_preprocessing`, `experiments`) |
| `notebooks-legacy/` | Older notebook exports |
| `src-legacy/` | Former `src/neuro` package (nilearn ROI helpers, TF models, etc.) |
| `scripts-legacy/` | One-shot notebook generators (`build_notebooks.py`, `make_notebook.py`, …) |
| `ADR/` | Architecture decision notes from the design phase |

**Active path:** `marimo_notebooks/` + `scripts/{prepare_real_features,run_ml_bakeoff,gen_book_data}.py` + `data/processed/`.
