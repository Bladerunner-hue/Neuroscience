venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data: large NIfTI stay local; processed tables are committed for the book
data/*
!data/processed/
!data/processed/**
!data/.gitkeep
# Allow a short local-data note under data/raw/ (not the BIDS tree)
!data/raw/
data/raw/**
!data/raw/README.md
*.nii.gz

# ML / notebooks clutter
figures/
mlruns/
mlflow.db*
notebooks/html/

# Local editor / agent tooling (never commit)
.vscode/
.idea/
.continue/
.grok/
.claude/
.cursor/
.codex/
.aider*
AGENTS.md
CLAUDE.md

# Accidental absolute-path dumps
home/

# marimo runtime (sessions are regenerated; keep folder via export)
marimo_notebooks/__marimo__/
marimo_notebooks/__pycache__/

# Optional: uncomment to stop tracking huge WASM assets and rely on CI only
# docs/wasm/
# marimo_exports/wasm/

# Spark local spill + Hadoop CRC sidecars
.spark-tmp/
*.crc

# God-mode TF training artifacts (regenerate via CLI)
checkpoints/
saved_model/
logs/

# Streaming runtime (checkpoints can be large / local)
data/processed/stream_checkpoints/
data/processed/stream_inbox/
data/processed/stream_features/
