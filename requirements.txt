# Public book (marimo WASM-compatible stack)
# Pin: Vite chunk hashes in docs/wasm/shared-assets must stay coherent with CI
marimo[recommended]==0.23.10
numpy
pandas
polars
scipy
scikit-learn
matplotlib
seaborn

# Local gateway: static WASM book + JSON feature API
fastapi
uvicorn[standard]

# Feature extraction from NIfTI (local)
nibabel
openneuro-py

# Chapter V — TensorFlow neural nets (local only; not on GitHub Pages WASM)
tensorflow[and-cuda]==2.20
# tensorflow-probability  # optional

# God-mode Spark (Tungsten/Catalyst honour — CPU only in JVM)
pyspark>=4.1.1
pyarrow
msgspec
typer[all]
# Optional Spark Connect client pin (use with docker-compose.spark.yml):
# pyspark[connect]==3.5.1

# Optional / research (not required for the book)
# mne  # production adaptive multitaper: prepare_real_features.py --psd mne
# cupy  # optional CUDA FFT in pre_ingest only (outside Spark)
# nilearn
# mlflow
# jupyterlab
